[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "shell"

[dependencies]
argon-user = { path = "../argon-user" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1
